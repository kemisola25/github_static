//USING FOR LOOP ADD 5000 TO EACH ITEM IN THE SCORE ARRAY

let scores = [4500, 6900, 8000, 34100, 5470, 5670];
console.log(scores);

for ( let i = 0; i < scores.length; i++);{
    scores[i] = scores[i] + 5000;
    console.log(scores);
}
